"""Geometry import + conversion utilities (KML reader, etc.).

Note: this is deliberately ``frisket.geometry``, not ``frisket.geo`` — the
latter name was retired by geo-projection-host-rehome-v1 (its projection code
now lives in ``frisket.projections``) and is guarded absent by
``tests/test_projection_point_backend_generic.py::test_geo_package_is_gone``.
"""
